# Purpose

This repository is an Android assessment app. The objective is to implement a minimal, correct product search flow using the existing app structure.

---

# Operating System

This repository is executed primarily on Windows.

Always prefer:

* PowerShell scripts (`.ps1`)
* `powershell -ExecutionPolicy Bypass -File ...`
* `.\gradlew.bat`

Do not assume:

* bash
* WSL
* Git Bash
* Unix utilities like `chmod`, `sed`, `grep`, or `rm`

---

# Hard Constraints

* Use Kotlin
* Use Jetpack Compose
* Use the existing `ProductService`
* Do not introduce Repository or UseCase layers
* Prefer minimal changes over broad rewrites
* Keep the solution simple and aligned with the assessment

---

# Required Workflow

For implementation tasks, always follow:

1. Inspect `README.md` and relevant source files
2. Plan the smallest viable change set
3. Implement incrementally
4. Run validation
5. Repair based on validation failures
6. Re-run validation
7. Produce a final review summary

Do not skip validation or final review.

---

# Harness Control Zone

The following files are part of the harness control plane and must not be edited during normal implementation work:

* `CLAUDE.md`
* `.claude/settings.json`
* `.claude/settings.local.json`
* `.claude/agents/**`
* `.claude/skills/**`
* `scripts/validate.ps1`
* `scripts/require-validation-report.ps1`
* `scripts/write-validation-report.ps1`
* `scripts/block-dangerous-command.ps1`
* `scripts/ensure-claude-dirs.ps1`

If one of these files must change, treat that as a separate harness-maintenance task, not as feature implementation.

---

# Validation (Canonical Definition)

The following commands define the canonical validation contract for this repository:

* `.\gradlew.bat :app:compileDebugKotlin --rerun-tasks --no-build-cache --console=plain`
* `.\gradlew.bat :app:testDebugUnitTest --rerun-tasks --no-build-cache --console=plain`
* `.\gradlew.bat :app:detekt --rerun-tasks --no-build-cache --console=plain`

These commands must be executed for any meaningful code change.

---

# Validation (Execution Method)

Validation must be executed only via:

* `powershell -ExecutionPolicy Bypass -File .\scripts\validate.ps1`

## Requirements

* `validate.ps1` is the single source of truth for validation execution and validation reporting
* `validate.ps1` must internally execute the canonical Gradle commands above
* `validate.ps1` must write `.claude/reports/validation-status.json` during the same run
* `validate.ps1` must write `.claude/reports/validation-raw.log` during the same run
* the validation report must not be created or edited manually
* the validation report must not be created by any script other than `validate.ps1`
* the script must fail fast on errors
* the script must produce deterministic results

Running Gradle commands directly is allowed only for local diagnosis, not for claiming final completion.

A task is not complete unless:

1. `validate.ps1` has been executed successfully
2. the validation artifact has been generated by that same run
3. the validation gate accepts the artifact as current and consistent

---

# Validation Artifact

Every validation run must produce:

* `.claude/reports/validation-status.json`
* `.claude/reports/validation-raw.log`

## Required format

```json
{
  "timestamp": "ISO-8601",
  "started_at": "ISO-8601",
  "finished_at": "ISO-8601",
  "duration_ms": 12345,
  "generator": "scripts/validate.ps1",
  "schema_version": 3,

  "compile": "PASS | FAIL | NOT_RUN",
  "tests": "PASS | FAIL | NOT_RUN",
  "detekt": "PASS | FAIL | NOT_RUN",
  "overall": "PASS | FAIL",
  "summary": "human-readable summary",

  "raw_log_path": ".\\.claude\\reports\\validation-raw.log",

  "steps": {
    "compile": {
      "status": "PASS | FAIL | NOT_RUN",
      "command": ".\\gradlew.bat :app:compileDebugKotlin --rerun-tasks --no-build-cache --console=plain",
      "exit_code": 0,
      "started_at": "ISO-8601",
      "finished_at": "ISO-8601",
      "duration_ms": 1000,
      "task_outcome": "EXECUTED | UP_TO_DATE | FROM_CACHE | NO_SOURCE | UNKNOWN"
    },
    "tests": {
      "status": "PASS | FAIL | NOT_RUN",
      "command": ".\\gradlew.bat :app:testDebugUnitTest --rerun-tasks --no-build-cache --console=plain",
      "exit_code": 0,
      "started_at": "ISO-8601",
      "finished_at": "ISO-8601",
      "duration_ms": 1000,
      "task_outcome": "EXECUTED | UP_TO_DATE | FROM_CACHE | NO_SOURCE | UNKNOWN"
    },
    "detekt": {
      "status": "PASS | FAIL | NOT_RUN",
      "command": ".\\gradlew.bat :app:detekt --rerun-tasks --no-build-cache --console=plain",
      "exit_code": 0,
      "started_at": "ISO-8601",
      "finished_at": "ISO-8601",
      "duration_ms": 1000,
      "task_outcome": "EXECUTED | UP_TO_DATE | FROM_CACHE | NO_SOURCE | UNKNOWN"
    }
  }
}